/// <reference path=".vscode/objects/person.ts" />

//app.ts uses typescript, so we get CODE HINTING!!!
//tsc app.ts in command line makes app.js
if($(document).ready())

var alex:objects.Person = new objects.Person("Alex");
alex.sayName();